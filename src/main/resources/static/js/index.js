$(function($) {
    //通过遍历给菜单项加上data-index属性
    // $(".J_menuItem").each(function (index) {
    //     if (!$(this).attr('data-id')) {
    //         $(this).attr('data-id', index);
    //     }
    // });
    $.optiontab.init();
});